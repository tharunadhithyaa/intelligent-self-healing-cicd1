{{- if .Values.createNamespaceResource }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "civicpulse.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
